import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Top 6 ATS-Friendly Resume Templates for Software Engineers (2026)",
  description: "Tested single-page ATS-optimized resume templates in Markdown and LaTeX with real-time preview and scoring.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
