import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Personalized Settings & Offline Backup Config (2026)",
  description: "Manage your Hirenza preferences, target company configurations, and offline data backup.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
